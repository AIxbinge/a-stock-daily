# A 股日报 - 接口测试用例

## 1. 接口连通性测试

### 1.1 行情接口连通性
| 用例 ID | TC-API-001 |
|---------|------------|
| 接口 | GET /api/quote/{stock_code} |
| 测试点 | 获取个股行情接口连通性 |
| 请求示例 | `GET /api/quote/600519` |
| 预期响应 | 200 OK，返回 JSON 格式数据 |
| 优先级 | P0 |

### 1.2 大盘指数接口连通性
| 用例 ID | TC-API-002 |
|---------|------------|
| 接口 | GET /api/index |
| 测试点 | 获取大盘指数接口连通性 |
| 请求示例 | `GET /api/index` |
| 预期响应 | 200 OK，返回三大指数数据 |
| 优先级 | P0 |

### 1.3 排行榜接口连通性
| 用例 ID | TC-API-003 |
|---------|------------|
| 接口 | GET /api/ranking/{type} |
| 测试点 | 获取排行榜接口连通性 |
| 请求示例 | `GET /api/ranking/change_percent` |
| 预期响应 | 200 OK，返回排序后的股票列表 |
| 优先级 | P0 |

### 1.4 搜索接口连通性
| 用例 ID | TC-API-004 |
|---------|------------|
| 接口 | GET /api/search?q={keyword} |
| 测试点 | 股票搜索接口连通性 |
| 请求示例 | `GET /api/search?q=茅台` |
| 预期响应 | 200 OK，返回匹配的股票列表 |
| 优先级 | P0 |

## 2. 响应数据验证

### 2.1 行情数据字段完整性
| 用例 ID | TC-API-005 |
|---------|------------|
| 接口 | GET /api/quote/{stock_code} |
| 测试点 | 验证返回字段完整性 |
| 验证字段 | code, name, price, change, change_percent, open, high, low, close, volume, amount, time |
| 预期结果 | 所有必填字段存在，类型正确 |
| 优先级 | P0 |

### 2.2 数据类型验证
| 用例 ID | TC-API-006 |
|---------|------------|
| 接口 | 所有接口 |
| 测试点 | 验证字段数据类型 |
| 验证内容 | 数值字段为 number 类型，时间字段为 string 或 timestamp |
| 预期结果 | 数据类型符合 Schema 定义 |
| 优先级 | P1 |

### 2.3 数值精度验证
| 用例 ID | TC-API-007 |
|---------|------------|
| 接口 | GET /api/quote/{stock_code} |
| 测试点 | 价格字段精度 |
| 验证内容 | 价格保留 2 位小数，涨跌幅保留 2 位小数 |
| 预期结果 | 精度符合金融数据规范 |
| 优先级 | P1 |

## 3. 异常场景测试

### 3.1 无效股票代码
| 用例 ID | TC-API-008 |
|---------|------------|
| 接口 | GET /api/quote/{stock_code} |
| 测试点 | 查询无效股票代码 |
| 请求示例 | `GET /api/quote/999999` |
| 预期响应 | 404 Not Found 或 200 + error 字段 |
| 优先级 | P0 |

### 3.2 参数缺失
| 用例 ID | TC-API-009 |
|---------|------------|
| 接口 | GET /api/quote |
| 测试点 | 缺少必需参数 |
| 请求示例 | `GET /api/quote` (无 stock_code) |
| 预期响应 | 400 Bad Request |
| 优先级 | P1 |

### 3.3 非法参数格式
| 用例 ID | TC-API-010 |
|---------|------------|
| 接口 | GET /api/quote/{stock_code} |
| 测试点 | 参数格式错误 |
| 请求示例 | `GET /api/quote/abc` |
| 预期响应 | 400 Bad Request |
| 优先级 | P1 |

### 3.4 搜索空结果
| 用例 ID | TC-API-011 |
|---------|------------|
| 接口 | GET /api/search?q={keyword} |
| 测试点 | 搜索无匹配结果 |
| 请求示例 | `GET /api/search?q=不存在的股票` |
| 预期响应 | 200 OK，返回空数组或提示 |
| 优先级 | P1 |

## 4. 性能测试

### 4.1 接口响应时间
| 用例 ID | TC-API-012 |
|---------|------------|
| 接口 | 所有接口 |
| 测试点 | 单次请求响应时间 |
| 预期结果 | P95 < 500ms，P99 < 1000ms |
| 优先级 | P0 |

### 4.2 并发请求测试
| 用例 ID | TC-API-013 |
|---------|------------|
| 接口 | GET /api/quote/{stock_code} |
| 测试点 | 并发请求处理能力 |
| 测试条件 | 10/50/100 并发 |
| 预期结果 | 无失败请求，响应时间在可接受范围 |
| 优先级 | P0 |

### 4.3 限流策略验证
| 用例 ID | TC-API-014 |
|---------|------------|
| 接口 | 所有接口 |
| 测试点 | 触发限流机制 |
| 测试步骤 | 短时间内发送大量请求（超过限流阈值） |
| 预期响应 | 429 Too Many Requests |
| 优先级 | P1 |

## 5. 数据源接口测试

### 5.1 第三方 API 连通性
| 用例 ID | TC-API-015 |
|---------|------------|
| 测试点 | 验证数据源 API 可用性 |
| 数据源 | 新浪财经/腾讯财经/东方财富等 |
| 预期结果 | 数据源可正常访问 |
| 优先级 | P0 |

### 5.2 数据一致性验证
| 用例 ID | TC-API-016 |
|---------|------------|
| 测试点 | 对比多数据源一致性 |
| 测试步骤 | 同时查询多个数据源，对比同一股票数据 |
| 预期结果 | 数据差异在合理范围内（< 1%） |
| 优先级 | P2 |

---

## 📊 接口测试用例统计
| 类别 | 用例数 | P0 | P1 | P2 |
|------|--------|----|----|----|
| 连通性 | 4 | 4 | 0 | 0 |
| 数据验证 | 3 | 1 | 2 | 0 |
| 异常场景 | 4 | 1 | 3 | 0 |
| 性能测试 | 3 | 2 | 1 | 0 |
| 数据源 | 2 | 1 | 0 | 1 |
| **总计** | **16** | **9** | **6** | **1** |
